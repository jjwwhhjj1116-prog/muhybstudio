# AI 에이전트 실행 계약

이 저장소를 사용하는 모든 AI 에이전트는 다음을 지킨다.

1. 작업 전 `workflow/00_BOOT_SEQUENCE.md`를 실제로 읽는다.
2. 사용자의 최신 확정 지시를 가장 높은 권한으로 취급한다.
3. 작품 사실은 `projects/<project>/PROJECT_CONTEXT.md`에서만 읽고 범용 지침에 섞지 않는다.
4. 현재 단계의 `stages/*.md`를 읽지 않은 채 산출물을 생성하지 않는다.
5. 긴 작업은 청크별 별도 요청으로 실행하고 각 청크 직후 상태 JSON을 저장한다.
6. 출력 청크를 회차·에피소드로 오해하지 않는다. 재훅, 재인트로, 중간 CTA, 가짜 엔딩을 만들지 않는다.
7. 상위 산출물 변경 시 `workflow/05_INVALIDATION.md`에 따라 후속 단계를 무효화한다.
8. 검수 오류를 숨기지 않는다. `FAILED` 또는 `NEEDS_REVIEW` 상태와 재개 지점을 기록한다.
9. 사용자 승인 없이 다음 청크를 계속하라는 프로젝트 설정이 있으면 청크별 검수 후 즉시 다음 청크를 실행한다. 실제 시간 지연은 필요하지 않으며 요청 분리와 체크포인트로 출력 제한을 제어한다.
10. Google Docs를 편집할 때 기존 탭과 내용을 먼저 읽고, 같은 문서 안에 단계별 탭 또는 명확한 섹션으로 누적한다.

## 금지

- 기억·대화 요약만으로 정본을 대체
- 이미 승인된 설정을 문장 편의를 위해 변경
- 대사 누락, 장면 수 맞추기용 허위 문장, 범용 오류 문구 삽입
- Flow 수동 재작성
- 영상 프롬프트에 오디오 요소 삽입
- 보조 인물 이름만 적어 이미지 생성 모델에 맡김
- 원격 Git 상태를 확인하지 않은 강제 푸시
