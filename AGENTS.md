# 무협스튜디오 작업 계약

1. 시작할 때 workflow/00_BOOT_SEQUENCE.md와 workflow/12_THREE_PC_SYNC.md를 읽고 Git 원격·브랜치·작업 상태를 확인한다. 최신 사용자 지시가 우선한다.
2. 이 저장소의 기준은 V13이다. 현재 단계의 stages 문서와 연결된 policy 문서만 읽는다. 작가 입력은 policy/01_WRITING.md와 필요한 작품 사실·직전 본문으로 제한한다.
3. 작품 정본·초안·인물 기억·미공개 자산은 별도 비공개 프로젝트에서 관리한다. 공개 저장소에 섞지 않는다. summary는 원문 근거를 대신하지 않는다.
4. 집필 뒤 인과·발화를 따로 검수하고 근거를 남긴다. 검수 전 초안에서 추출한 사실을 기억의 accepted 상태로 저장하지 않는다. 단어 빈도·문장 길이·대화 줄 수·상시 예문으로 문체를 통제하지 않는다.
5. 기존 웹툰 디자인을 유지한다. 의미 장면·샷·키프레임·생성 작업을 구별하고 실제 시작/종료 이미지와 중간 동작을 검수한다. Flow 모델이 얼굴을 유지하거나 실패한다고 시험 전에 단정하지 않는다.
6. 청크별 파일·상태·입력/출력 해시와 다음 작업을 남긴다. 사용자가 허용한 작업을 청크마다 재승인받지 않는다. 부재 입력이 필요한 단계만 대기한다.
7. 사용자가 이 저장소의 지속적인 커밋·푸시를 요청했다. 검증된 지침·코드 변경은 작업 브랜치에서 커밋한 뒤 main에 통합하고 push 결과와 원격 SHA를 확인한다. 로컬 파일만 만들고 공유 완료라고 하지 않는다. 일반적인 동일 범위의 push를 매번 재승인받지 않는다.
8. 다른 PC의 변경은 먼저 fetch해 확인한다. 충돌이 없으면 검증 후 통합하며 force push·다른 작업 덮어쓰기·자동 stash는 하지 않는다. 미완료 작업은 별도 브랜치로 푸시하고 재개 위치를 coordination/STATUS.md에 적는다.
9. 커밋에 포함할 경로를 명시한다. git add . / -A / --all을 사용하지 않는다. 원격에 올릴 수 없는 작품 내용과 인증정보를 먼저 분리한다.
10. 문서·도구의 테스트·실제 생성·최종 렌더의 확인 수준을 구분한다. legacy/v12의 실행 도구는 새 제작에 사용하지 않는다. 전체 자동화가 구현됐다고 과장하지 않는다.
